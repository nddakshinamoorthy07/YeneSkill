import { beforeAll, afterEach, afterAll } from 'vitest';

beforeAll(() => {
  // Setup mock Firebase if needed
});

afterEach(() => {
  // Clean up after each test
});

afterAll(() => {
  // Final cleanup
});
